/* function item(name, bg, cl) {
    return `<div class="item">` +
        `<div class="item-img ${cl}" style="background:${bg}"></div>` + // class name and background image
        `<div class="item-desc item-name">${name}</div>` + // item name
        `<div class="item-desc item-quantity">Quantity: 10</div></div>`;
} */